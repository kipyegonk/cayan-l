services:
  - type: web
    name: cayan-api
    env: php
    buildCommand: composer install --no-dev && php artisan config:clear && php artisan migrate --force
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
